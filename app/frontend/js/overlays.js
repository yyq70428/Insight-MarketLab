export function analysisSummary(result){const patterns=result.harmonics||[],zones=result.zones||[];const pattern=patterns[0];return `<span class="analysis-chip">${pattern?`${pattern.name} · ${pattern.direction==="bullish"?"看漲":"看跌"} ${pattern.score}`:"未偵測諧波"}</span><span class="analysis-chip">${zones.filter(z=>z.type==="support").length} 支撐 · ${zones.filter(z=>z.type==="resistance").length} 壓力</span><span class="analysis-chip">ZigZag ${result.pivots?.length||0} 點</span>`}
