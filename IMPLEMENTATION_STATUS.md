# MarketLab 實作與驗收狀態（2026-09-12）

本表區分程式接通、真實服務驗證，以及尚未執行的外部操作，不把 UI 佔位或不完整觀察期列為成功。

| 階段 | 狀態 | 內容與證據 |
| --- | --- | --- |
| 1. 專案骨架與環境 | 已實作 | 單一設定層、依賴、環境範例、機密排除 |
| 2. 市場資料與 API | 已實作、已連線 | Yahoo K 線／報價／搜尋、OHLCV 驗證、時區與錨點截斷、盤中保留期間限制 |
| 3. 分析引擎 | 已實作、有自動測試 | 諧波比例、近期與歷史支撐區間、RSI／MACD 相對位置與追價防護、權重正規化；技術風險空間採 ATR |
| 4. 主介面 | 已接通、桌面回歸通過 | 側欄收窄、支撐區間簡化；1280／1366／1536／2048 無頁面溢出。未宣稱六張參考圖逐像素完全相同 |
| 5. 錨點與單次 Agent | 已實作、真實流程通過 | 證交所身分、分輪歷史新聞、原文雙時間驗證、真實模型結構化報告、凍結決策、紙上驗證、回放／還原 |
| 6. 持久化、版本與後台 | 已實作、Mongo 整合通過 | 背景 job、四階段、唯一 champion、不可變版本、revision、回滾、候選、非重疊 shadow、可選語意評審、五頁籤與明細 |
| 7. 區間序列回測 | 已實作、真實兩日批次通過 | 逐交易日、前後輪串接、版本時點隔離、重載恢復、不完整結果不算成功、後續行情更新 |
| 8. 掃描／告警／個股／量化 | 已實作與接線 | 產業篩選、可選 Discord 發送與 PostgreSQL 去重、雙新聞、2 年／6 月 walk-forward、基準比較、±20% 擾動、9 份離線快照 |
| 9. 容器與安全 | 已部署、健康檢查通過 | 獨立 Compose 專案 insight-marketlab；9021／9022／9023；保留原 Mongo／PostgreSQL volume |

## 真實服務驗證

- 0050.TW，2026-09-09～2026-09-12：批次 6debdfe92346444aad42bfd98c9c0f3e。
  - 來源當時僅提供 9/9、9/10 兩個錨點；兩輪四階段皆完成。
  - 兩輪皆使用真實模型，新聞各 8 篇。
  - 最長持有 5 根，後續行情不足，因此 waiting_validation、已驗證 0 輪，不計成功率。
- 0050.TW，2026-09-01～2026-09-02：批次 f7fa422f6a6745d5b6a538471afa03f1。
  - 兩輪全部分析及紙上驗證完成，第二輪建立時間晚於第一輪完成時間。
  - 重新整理恢復批次，後台五頁籤無瀏覽器執行錯誤。
- 0050.TW，2026-09-03：最終瀏覽器驗收 Session cc7e8a2d6b384743b26dd5cad7c5529f。
  - 技術與新聞完成後，真實模型決策、紙上驗證與後端自動自適應階段均完成。
  - 已驗證回放、回到錨點、重新載入恢復、量化頁及輪次明細。重載時必須等正確錨點行情到齊才能回放；另驗證時序嚴格遞增且不重疊，避免最新行情與歷史回放重複造成 K 線索引錯誤。
  - 刻意攔住錨點 K 線回應的競態回歸已通過；按鈕與程式皆阻擋提早回放，資料抵達後恢復。完整瀏覽器檢查錯誤 0。
- 獨立語意評審真實模型呼叫成功。正式環境仍維持原本 RAGAS_ENABLED=false，未自動啟用。
- 離線研究：0050.TW、AAPL、BTC-USD × ma／rsi／bollinger，共 9 份快照；各有非重疊驗證窗與擾動結果。

## 自動測試

完整容器測試曾通過 90 passed、0 skipped、0 failed，另有 Starlette 相依套件的棄用警告。最後的歷史區間價位分散與畫面篩選調整，另在主機執行 10 項分析／歷史區間測試通過；前端資料／區間選擇測試 10 passed。其後再次執行完整套件時遭下述記憶體事件中止，不能把該次重跑計為通過。

涵蓋錨點／修改時間／引用、模型失敗、Mongo 持久化、角色競態、決策前禁止未來行情、未完成 K 線、逐輪順序、94 日含起迄上限、覆寫、版本時點／revision／回滾、影子非重疊與忠實度門檻、量化下一開盤／成本／訓練隔離、JSON 可序列化、跨來源價格斷層、真實暴跌保留、缺日揭露、告警去重及預設停用。

~~~bash
docker compose -f deploy/docker-compose.yml --env-file .env exec -T app python -m pytest app/backend/tests -q -p no:cacheprovider
node --test app/frontend/tests/*.cjs
~~~

瀏覽器檢查在 scripts/check-flow.cjs、scripts/check-connected-pages.cjs、scripts/check-workbench.cjs。真實測試會建立持久化紙上回測記錄並消耗模型 API 用量，不會建立真實交易。證據存於本機 artifacts/flow-check/ 與 artifacts/workbench-check/，已排除版本與映像。

## 技術卡與歷史區間增補驗收

- `scripts/check-technical-report.cjs`：0050.TW、2026-09-01 真實技術報告，已驗證多空、四項理由、價格／百分比一致、MACD 位置、週末日期、舊報告相容、文字跳脫及重新分析建立新輪次。1536／2048／1280 無水平溢出，側欄不超過 540 px；瀏覽器錯誤 0。此腳本僅執行技術階段，不呼叫新聞或模型。證據在 `artifacts/technical-report/`。
- `scripts/check-zone-history.cjs`：2330.TW、2026-09-03，驗證 2,431 根錨點內真實行情、末價 2,390、歷史候選上限、平移至舊年份、全歷史縮放、開關、窄版與重疊去除。可用 `MARKETLAB_TEST_URL` 指定預覽位置，預設 9021。證據在 `artifacts/zone-history/`。
- 驗證時發現行情併發串用風險：2330 曾取得 228.45 與較短的 496 根期間；單獨下載及原服務重抓均為 2,390、2,431 根。已加入抓取互斥、MultiIndex ticker 檢查與錯標的不進快取測試。原有報告不自動覆寫，不能視為已追溯驗證所有歷史報告。
- 新 MACD 80/20 位置折減是工程規則，不是原生 MACD 界限或已證實的最佳參數。目標根數仍為 5 根，日線日期只排除週末，未套用交易所休市日曆。
- 歷史框為分段回看整理，不是當時即可用的交易訊號；仍受實際載入歷史長度限制，不能顯示來源未提供的年代。
- 最新版已於正式 9021 完成歷史圖表與 1280／1366／1536／2048 版面回歸，沒有水平／垂直頁面溢出或瀏覽器錯誤。實际映像為 `3195761267cb`，健康檢查正常；歷史圖表證據的 `base` 為 `http://127.0.0.1:9021`。

## 本次驗收的資源事件

2026-09-12 14:14（台北時間），Colima 虛擬機總記憶體約 1,966 MB、沒有 swap；既有多個服務與額外的測試／隔離預覽並行時，可用記憶體降至約 19 MB。核心 OOM 記錄確認正式 app 的 uvicorn 被系統終止，容器自動重啟。

- 使用者批次 `a3b8ec00c4cf46bebeda413a439c0c6a` 被標記 `interrupted`，18／24 輪已完成，當時錨點 2026-08-27。原報告仍保留，未自動重跑或消耗額外模型費用。
  - 尚未完成的錨點為 2026-08-27、08-28、08-31、09-01、09-02、09-03。接續需另行建立執行工作；不改寫原批次為成功。
- 僅本次測試與預覽容器被停止／移除；沒有刪除正式 Mongo／PostgreSQL 資料卷，也沒有停止其他專案。
- 記憶體恢復後，9021／Mongo／PostgreSQL 健康檢查成功。確認沒有活動工作後，才將新版映像套用至既有 app。
- 未調整整台 Colima 的資源或重啟虛擬機；這會影響其他專案，需要使用者另行決定。低記憶體環境不要同時執行額外應用副本、完整測試與回測。

## 限制與明確未做的操作

- 沒有真實下單功能，交易皆為紙上模擬。
- 沒有發送真實 Discord 通知；使用者需設定 webhook 並開啟 ALERTS_ENABLED=true。成功後記錄與冷卻阻擋已用替身測試。
- 真實自動策略提升需要至少 8 個成熟且不重疊影子樣本。不能用本次少量測試宣稱已證明改善或獲利；門檻與發布隔離已用自動測試驗證。
- 行情缺少後續資料時保持等待，日後按「更新後續行情與驗證」；不改寫已凍結決策、不填入假成功。
- 真實來源仍可能暫時逾時或限流；本次驗收遇過 Yahoo 短暫逾時，後續請求恢復。資料失敗時不使用人造 K 線冒充連線成功。
- 現存原文與發布／修改時間可驗證，但不是具有版本歷史的第三方封存；無法驗證的文章一律排除。
- 使用規格指定的單進程背景執行器。重載瀏覽器不影響完整工作；重啟 app 中斷的工作標記 interrupted／failed，不自動重放。
- FinMind 不可用，或大幅價格斷層經同交易日 Yahoo 交叉比對不一致時，明示整段回退 Yahoo；不拼接或猜測拆股比例調價。對照不足、涵蓋少於 95% 時拒絕計算；實際期間與缺日數寫入快照並顯示。兩來源皆有的真實大跌不會被修平。此為大幅斷層防護，並非完整公司行動總報酬引擎；未計入現金股息再投資。
- 量化擾動「穩健」判準為最後驗證窗各組相較參考組下降不超過 5 個百分點，UI 明示全部組合與判準，非未來績效保證。

## 0050 拆股資料修正

證交所公告確認 0050 於 2025-06-18 以 4 倍分割恢復交易，2025-06-10 收盤 188.65 對應分割後參考價 47.16。[證交所公告](https://www.twse.com.tw/zh/ETFortune/announcement?company=A00005&date=20250617&fund=0050&seq=1&type=other)

本機實際比對：FinMind 6/18 開盤相對上一交易日收盤 −74.8211%，Yahoo 同期 +0.7156%；前者不是可直接拿來計算持倉報酬的同基準價格序列。0050 三份快照已重算，來源與品質檢查保留在 JSON。Yahoo 本次實際期間為 2021-09-13～2026-09-10，較 FinMind 少 9/11 一個交易日，不填補。
